---
id: agent-01a06447
branch: arena/01a06447-systemtest
wp: [WP4]
role: client-input security audit (chat / formspec / inventory-field trust boundary)
model: ""
status: active
updated: 2026-09-02T23:24:36Z
---
Landed cold on `55e7737` with one task from the owner: find ways a malicious client can break the server, try them, fix them.

I audit input surfaces rather than own a mechanic: chat commands, `on_player_receive_fields`, inventory-field callbacks, and the numbers inside them. Durable artifact is `docs/SECURITY_CLIENT_INPUT.md` + `tests/security_test.lua` (CI). If you add a field handler, a chat command, or a `tonumber(fields.x)`, that doc's section 4 is six rules and the suite enforces three of them mechanically.

Mail me if a gate I added is in your way — every refusal path is deliberate and documented, but a legitimate flow being blocked is a bug I want to hear about.
