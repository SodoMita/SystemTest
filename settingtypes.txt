zash_infplace (Infinite node placement) enum auto true,false,auto

# If enabled, new players receive some initial items when joining for the first time.
zash_give_initial_stuff (Give initial stuff) bool true

# If enabled, automated tests of the Lua API such as player health, crafting, and PseudoRandom will be performed on startup.
zash_unittests_autostart (Perform unit tests) bool false

# Adjust the volume of the background music
zash_music_gain (Music gain) float 0.5 0.0 1.0

# Automatically start matches when the lobby has enough players (no ready
# prompts). The ready-check flow stays available via /sl_match_start.
sl_auto_start (Auto-start matches) bool false

# Lobby intermission in seconds before an auto-started match countdown.
sl_auto_start_delay (Auto-start intermission) int 8 3 120

# Interior side length, in nodes, of the hollow cubes in the auto-generated
# neon grid arena. The wall grid is one node thick and as tall as the cubes
# are wide, so each cell is a sealed hollow cube with one penned monster.
sl_arena.cube_size (Arena cube size) int 4 2 16

# Number of monster cubes along the X (east-west) axis of the arena.
sl_arena.grid_width (Arena grid width in cubes) int 5 2 32

# Number of monster cubes along the Z (north-south) axis of the arena.
sl_arena.grid_depth (Arena grid depth in cubes) int 3 2 32

# Generate an infinite flat neon grid floor at ground level (y = 0) under
# every generated chunk, so players can never walk off the map and fall
# into the void. The floor is one node thick; the arena cube grid sits on
# top of it like an island on an endless plane.
sl_arena.infinite_floor (Infinite flat neon floor) bool true
