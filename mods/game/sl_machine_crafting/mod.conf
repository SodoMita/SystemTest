name = sl_machine_crafting
description = Machine crafting stations: the Objective Forge (placeables come only from machines, never from the inventory)
depends = sl_modebase, sl_texgen
optional_depends = sl_gui
