name = sl_chatplay
description = Chatplay: every role tool playable through text commands; headless agent console player + HTTP/file transport; text HUD/vision
author = SodoMita
depends = sl_modebase
optional_depends = aaa_botmatch, sl_weapons, sl_gui
min_minetest_version = 5.0
