name = aaa_botmatch
description = Headless soak-test harness - simulated AI players drive real matches on a live Luanti server, harvesting bugs and balance telemetry into botmatch_stats.json. Inert unless sl_botmatch.enabled = true. The aaa_ prefix guarantees load-before-all so callback registration can be intercepted.
optional_depends =
