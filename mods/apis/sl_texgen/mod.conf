name = sl_texgen
description = Runtime procedural texture generation: renders placeholder textures to PNG at server startup and pushes them as dynamic media, so the PNG files never have to ship in the game.
optional_depends =
