name = sl_texgen
description = Runtime procedural textures: compiles placeholder art into client-rendered [combine: modifier programs over tiny shared base textures (textures/stx_*.png), so no baked PNGs ship and the server never rasterizes pixels.
optional_depends =
